{
  "name": "nfcraft-public-card-site",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test worker.test.mjs",
    "check": "node --check worker.mjs"
  }
}
